[package]
name = "brainfuck"
version = "0.1.0"
edition = "2018"

[dependencies]
rustc-hash = "1.1.0"
structopt = "0.3"

[dev-dependencies]

newline-converter = "0.2"

[profile.release]
lto = true
codegen-units = 1

[profile.test]
opt-level = 3 # tests still work but are really slow without this
